# =====================================================================
# digital_twin_bot — runtime dependencies
# Python: 3.12+
# =====================================================================

# --- Telegram bot framework ---
aiogram==3.27.0

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2

# --- Database ---
# SQLite (aiosqlite) is enough for local dev — no external services.
# Prod uses PostgreSQL via docker compose (asyncpg + psycopg).
SQLAlchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Observability & resilience ---
structlog==24.4.0
tenacity==9.0.0

# --- HTTP client (to llama.cpp OpenAI-compatible API) ---
httpx==0.27.2

# --- Web search fallback (DuckDuckGo, no API key) ---
duckduckgo-search==6.3.7
# lxml is needed for the HTML and lite backends (the JSON API backend is
# aggressively rate-limited — we fall back through html → lite → api).
lxml>=5.0,<6

# --- RAG pipeline ---
pymupdf==1.24.13
sentence-transformers==3.3.1
numpy>=1.26,<3
tqdm==4.67.1
# .docx ingest for teacher-uploaded materials (lecture notes, hand-outs).
python-docx==1.2.0
# BM25 lexical retrieval for the hybrid (BM25 + dense) first stage. Catches
# exact-token matches (surnames, years, acronyms) that cosine normalises away.
rank-bm25==0.2.2

# --- Utilities ---
python-dotenv==1.0.1
